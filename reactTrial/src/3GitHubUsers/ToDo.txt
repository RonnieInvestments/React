Purpose of the project
 - learning react props - passing data from one component to another
 - react mapping - <array data>
 - useState - manage state in applications
 - useEffect - lifecycle of a state/project


Steps
 - Design
  1. index.jss (full application)
  2. users/main section {
     - index.jss : user entry point
     - singlerUser.jsx
     - map
  }

  3. - Folder inside src => 3GutHubUser
    in the folder create index.jsx file {
        component - name
        render - coming soon
    }

  4. create userSection that will have our users
   - user section coming soon
   - render this component inside the main App component

  5. copy the provided list of users in your project as json 

  6. create a single user card
   - create a file called SingleUser inside the folder 'UsersSection'
   - render information from a single user
   - copy one user object

  7. Display

  8. in UsersSection component - import full list of all users (users.json)
   - console.log (users) // check if you can see the full list

  9. in the user list, instead of the whole list - console.log specific index

  10. create a modular component 
     - create a singleuserV2 component
     - take in data (avatar_url, login) externally and display

     - create component and console.log (props)
      - favorite country : string
      - address : string
      - isMarried : Boolean
      - pet : {} object
      - colors : [] array

  11. pass real data now... avatar_url, login_url, 

  12. Map method

  13. Use the url to navigate 
    - instead of showing the url have a button that one can click
    - events -> click, hover, side effects
    - react offers events through callback f(x)

  14. The Search section (onChange event, state management)
    - take input from the user to search a specific person
    - text input have a search and button

  15. To learn state 
   - main section
   - numbers decrement or increment the numbers
   - folder (learn basic state) - current project temp
   - create file numbers
   - increment/ decrement buttons


